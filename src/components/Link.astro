---
interface Props {
  href: string;
  rel?: string;
}

const { href, rel } = Astro.props;
---

<a
  class="font-semibold underline underline-offset-2 hover:decoration-4 focus:decoration-4 focus:outline-none"
  href={href}
  rel={rel}><slot /></a
>
