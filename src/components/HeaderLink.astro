---
interface Props {
  href: string;
}

const { pathname } = Astro.url;
const { href } = Astro.props;
---

<a
  class:list={[
    "box hover:box-lg bg-neo-blue focus:box-lg text-xl font-bold underline decoration-4 underline-offset-8 focus:outline-none",
    {
      underline: pathname === href,
    },
  ]}
  href={href}
  data-astro-prefetch
>
  <slot />
</a>
