---
import Header from "@components/Header.astro";

import "@fontsource/ibm-plex-sans";
import "@fontsource/ibm-plex-mono";

interface Props {
  title?: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <script
      data-goatcounter="https://maywood.goatcounter.com/count"
      async
      src="//gc.zgo.at/count.js"
    ></script>
  </head>
  <body
    class="ctp-latte container mx-auto bg-ctp-base tracking-tighter text-ctp-text dark:ctp-mocha"
  >
    <Header />

    <main class="px-6 py-4">
      {title && <h1 class="text-4xl font-bold">{title}</h1>}
      {subtitle && <h2 class="text-lg italic">{subtitle}</h2>}

      <slot />
    </main>
  </body>
</html>
